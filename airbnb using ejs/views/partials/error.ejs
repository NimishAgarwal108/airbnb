<% if (typeof errors !== 'undefined' && errors.length > 0) { %>
  <% const fieldErrors = errors.filter(e => e.param === field); %>
  <% fieldErrors.forEach(error => { %>
    <p class="text-red-500 text-sm mt-1"><%= error.msg %></p>
  <% }); %>
<% } %>